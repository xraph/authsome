schemaVersion: 1
contributor:
  name: geofence
  envelope:
    supports: [v1]
    preferred: v1
  capabilities: [auth.read]

intents: []

graph:
  - route: /security/geofence
    intent: page.shell
    title: Geofencing
    nav: { group: Security, icon: map-pin, priority: 3 }
    slots:
      main:
        - intent: settings.panel
          props:
            namespace: geofence
            scope: app
