schemaVersion: 1
contributor:
  name: social
  envelope:
    supports: [v1]
    preferred: v1
  capabilities: [auth.read]

intents: []

graph:
  - route: /auth/social
    intent: page.shell
    title: Social Login
    nav: { group: Auth, icon: users, priority: 6 }
    slots:
      main:
        - intent: settings.panel
          props:
            namespace: social
            scope: app
