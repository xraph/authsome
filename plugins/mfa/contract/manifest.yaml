schemaVersion: 1
contributor:
  name: mfa
  envelope:
    supports: [v1]
    preferred: v1
  capabilities: [auth.read]

intents: []

graph:
  - route: /auth/mfa
    intent: page.shell
    title: Multi-Factor Auth
    nav: { group: Auth, icon: shield-check, priority: 4 }
    slots:
      main:
        - intent: settings.panel
          props:
            namespace: mfa
            scope: app
