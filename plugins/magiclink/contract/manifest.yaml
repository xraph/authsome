schemaVersion: 1
contributor:
  name: magiclink
  envelope:
    supports: [v1]
    preferred: v1
  capabilities: [auth.read]

intents: []

graph:
  - route: /auth/magiclink
    intent: page.shell
    title: Magic Link
    nav: { group: Auth, icon: link, priority: 3 }
    slots:
      main:
        - intent: settings.panel
          props:
            namespace: magiclink
            scope: app
