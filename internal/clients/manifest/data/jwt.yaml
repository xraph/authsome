plugin_id: jwt
version: 1.0.0
routes:
    - name: CreateJWTKey
      description: CreateJWTKey creates a new JWT signing key
      method: POST
      path: /createjwtkey
      request_type: jwt.CreateJWTKeyRequest
    - name: ListJWTKeys
      description: ListJWTKeys lists JWT signing keys
      method: GET
      path: /listjwtkeys
    - name: GetJWKS
      description: GetJWKS returns the JSON Web Key Set
      method: GET
      path: /jwks
    - name: GenerateToken
      description: GenerateToken generates a new JWT token
      method: POST
      path: /generate
      request_type: jwt.GenerateTokenRequest
    - name: VerifyToken
      description: VerifyToken verifies a JWT token
      method: POST
      path: /verify
      request_type: jwt.VerifyTokenRequest
types:
    - name: Plugin
      fields:
        "": Config
    - name: Config
      fields:
        accessExpirySeconds: int!
        includeAppIDClaim: bool!
        issuer: string!
        refreshExpirySeconds: int!
        signingAlgorithm: string!
    - name: Handler
      fields:
        "": '*jwt.Service'
